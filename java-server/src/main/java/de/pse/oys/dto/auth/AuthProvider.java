package de.pse.oys.dto.auth;

/**
 * AuthProvider – Enum für die verschiedenen externen Authentifikationsprovider, die unterstützt werden.
 *
 * @author uhupo
 * @version 1.0
 */
public enum AuthProvider {
    /**
     * Der Google Identity Provider (über OIDC).
     */
    GOOGLE,
}
