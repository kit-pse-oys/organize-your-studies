###############################################################################
# ORGANIZE YOUR STUDIES - LOKALE ENTWICKLUNG
# Kopiere als ".env.local" und passe bei Bedarf an.
# Zuletzt aktualisiert: 30-01-2026
# Author: uhupo
###############################################################################

# --- DATENBANK (PostgreSQL) ---
DB_HOST=db
DB_PORT=5432
DB_NAME=oys_db
DB_USER=postgres
DB_PASSWORD=devpassword

# --- JAVA BACKEND (Spring Boot) ---
SSL_ENABLED=false
SERVER_PORT=8080
SSL_KEYSTORE_PASSWORD=dummy
# Lokale Entwicklung ohne SSL daher leer lassen
SSL_KEYSTORE_PATH=

# --- Google OAuth Configuration ---
GOOGLE_OAUTH2_CLIENT_ID=dev-client-id
GOOGLE_CLIENT_SECRET=dev-client-secret
GOOGLE_CALLBACK_URL=http://localhost:8080/login/oauth2/code/google

# --- JWT CONFIGURATION ---
JWT_SECRET=myDefaultSecretHuber1234567890TestJWTSecretForDevPurposesOnly!!!
JWT_ACCESS_EXPIRATION=900000
JWT_REFRESH_EXPIRATION=3600000

# --- MICROSERVICE URL ---
MICROSERVICE_URL=http://oys-python-microservice:5001
