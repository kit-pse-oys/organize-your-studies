###############################################################################
# ORGANIZE YOUR STUDIES - UMGEBUNGSVARIABLEN (TEMPLATE)
# Kopiere diese Datei als ".env" und trage deine lokalen Daten ein.
# Die Datei ".env" darf NIEMALS in Git hochgeladen werden!
###############################################################################

# --- DATENBANK (PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=oys_db
DB_USER=postgres
DB_PASSWORD=dein_geheimes_passwort

# --- JAVA BACKEND (Spring Boot) ---
SERVER_PORT=8080

# --- Google OAuth Configuration ---
GOOGLE_OAUTH2_CLIENT_ID=deine-id-hier.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=dein_secret_hier
GOOGLE_CALLBACK_URL=http://localhost:8080/login/oauth2/code/google

# --- JWT CONFIGURATION ---
JWT_SECRET = dein_langes_geheimes_jwt_secret_hier
JWT_ACCESS_EXPIRATION=jwt_accessdauer
JWT_REFRESH_EXPIRATION=jwt_refreshdauer