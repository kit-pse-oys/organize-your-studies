###############################################################################
# ORGANIZE YOUR STUDIES - UMGEBUNGSVARIABLEN (TEMPLATE)
# Kopiere diese Datei als ".env" und trage deine lokalen Daten ein.
# Die Datei ".env" darf NIEMALS in Git hochgeladen werden!
###############################################################################

# --- DATENBANK (PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=oys_db
DB_USER=postgres
DB_PASSWORD=dein_geheimes_passwort

# --- JAVA BACKEND (Spring Boot) ---
SERVER_PORT=8080
# Falls das Java-Backend ein Secret für JWT-Tokens braucht:
JWT_SECRET=generiere_ein_zufälliges_langes_geheimnis_hier

# --- PYTHON MICROSERVICE (Flask / Solver) ---
# Port auf dem der Python-Service läuft
PYTHON_SERVICE_PORT=5000
# URL unter der das Java-Backend den Python-Service erreicht
PYTHON_SERVICE_URL=http://localhost:5000

# Google OAuth Configuration
GOOGLE_CLIENT_ID=deine-id-hier.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=dein-secret-hier
GOOGLE_CALLBACK_URL=http://localhost:8080/login/oauth2/code/google

# --- LOGGING & DEBUG ---
# Auf 'True' setzen für detaillierte Fehlermeldungen (nur lokal!)
DEBUG=True

